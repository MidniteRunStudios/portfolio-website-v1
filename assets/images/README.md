Place logo and image files in this folder and reference them from the HTML/CSS.

Recommended formats: `.png`, `.jpg`, `.svg`, `.webp`.

Example filename for the site logo:
- logo.png

After adding your logo file, you can replace the placeholder `assets/images/logo.png` used in `index.html` if you prefer a different name.